<?xml version="1.0" encoding="UTF-8"?>
<install>

	<sql><![CDATA[
		CREATE TABLE IF NOT EXISTS `k_shop` (
		  `id_shop` mediumint(64) NOT NULL auto_increment,
		  `allow_card` tinyint(1) NOT NULL,
		  `allow_cheque` tinyint(1) NOT NULL,
		  `allow_coupon` tinyint(1) NOT NULL,
		  `shopName` varchar(255) collate utf8_unicode_ci NOT NULL,
		  `shopApiFolder` varchar(255) collate utf8_unicode_ci NOT NULL,
		  `shopMailTo` varchar(255) collate utf8_unicode_ci NOT NULL,
		  `shopMailCc` varchar(255) collate utf8_unicode_ci NOT NULL,
		  `shopMailBcc` varchar(255) collate utf8_unicode_ci NOT NULL,
		  `shopMailTemplate` varchar(255) collate utf8_unicode_ci NOT NULL,
		  `shopMailTitle` varchar(255) collate utf8_unicode_ci NOT NULL,
		  `shopChequeOrder` varchar(255) collate utf8_unicode_ci NOT NULL,
		  `shopChequeAddress` mediumtext collate utf8_unicode_ci NOT NULL,
		  PRIMARY KEY  (`id_shop`)
		) ENGINE=MyISAM  DEFAULT CHARSET=utf8 COLLATE=utf8_unicode_ci;
	]]></sql>

	<preSript></preSript>
	<postScript></postScript>

</install>